#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    vector<string> generateParenthesis(int n) {
        vector<string> ans;
        
    }
};

int main(){
    int n = 3;
    Solution s;
    auto ans = s.generateParenthesis(n);
    for(auto str : ans){
        cout << str << endl;
    }
}