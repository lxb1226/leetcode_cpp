#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    int numIslands(vector<vector<char>>& grid) {
        auto m = grid.size();
        auto n = grid[0].size();
        vector<vector<bool>> isAccessed(m, vector<bool>(n, true));


    }

    void dfs(int i, int j,vector<vector<char>>& grid, vector<vector<bool>>& isAccessed, int num){
        
    }
};

int main(){

}